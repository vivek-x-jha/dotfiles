# https://docs.atuin.sh/

filter_mode_shell_up_key_binding = "directory"
style = "compact"
inline_height = 30
invert = true
enter_accept = true

[stats]
common_subcommands = [
  "apt",
  "cargo",
  "composer",
  "dnf",
  "docker",
  "git",
  "go",
  "ip",
  "kubectl",
  "nix",
  "nmcli",
  "npm",
  "pecl",
  "pnpm",
  "podman",
  "port",
  "systemctl",
  "tmux",
  "yarn",
]

common_prefix = ["sudo"]

[keys]

[sync]
records = true

[preview]

[daemon]

[theme]
name = "sourdiesel"
