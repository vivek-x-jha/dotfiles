# Load shell environment variables
source "$HOME/.dotfiles/.shellenv.sh"

# Configure shell options
export SHELL_SESSIONS_DISABLE=1
export ZAP_GIT_PREFIX='git@github.com:'
export ZDOTDIR="$XDG_CONFIG_HOME/zsh"
